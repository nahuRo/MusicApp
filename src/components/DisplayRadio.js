import { useEffect } from "react";

const DisplayRadio = () => {
	useEffect(() => {
		// albums.forEach((element) => {
		// 	fetchFromAPI(`/album/${element}`).then((resp) => {
		// 		console.log(resp);
		// 	});
		// });
	}, []);

	return <div>DisplayRadio</div>;
};

export default DisplayRadio;
